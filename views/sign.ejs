<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('head') %>
    </head>
    <body>
        <% if(type == 'signin') { %> <%- include('components/signin') %> <% }
        else { %> <%- include('components/signup') %> <% } %>
    </body>
</html>
